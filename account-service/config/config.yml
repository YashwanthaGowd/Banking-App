server:
  host: 0.0.0.0  # Changed from localhost
  port: 8080
postgres:
  host: postgres  # Changed from localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: banking
  sslmode: disable
  uri: postgres://postgres:postgres@postgres:5432/banking?sslmode=disable  # Updated host
kafka:
  brokers:
    - kafka:29092  # Changed to internal Kafka port
  topic: banking-transactions
  batch_size: 100
  batch_timeout: 10
  required_acks: -1
  async: false